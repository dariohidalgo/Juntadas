# Juntadas - Expense Sharing App

This project has been migrated to an Expo-based mobile and web application.

## 🚀 Active Project

The main application code, configuration, and assets are now located in the `juntadas-expo` directory.

### Getting Started

To run the application:

```bash
cd juntadas-expo
npm install
npm start
```

For more detailed information on building and deploying, see the [BUILD_GUIDE.md](file:///d:/app%20gastos/eternal-expanse/juntadas-expo/BUILD_GUIDE.md) inside the `juntadas-expo` directory.

## 📂 Project Structure

- `juntadas-expo/`: The core Expo application.
- `.git/`: Git history and configuration.
- `.gitignore`: Root-level git ignore rules.
